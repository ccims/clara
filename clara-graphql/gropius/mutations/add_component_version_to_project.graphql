mutation AddComponentVersionToProject($componentVersion: ID!, $project: ID!) {
    addComponentVersionToProject(input: {
        componentVersion: $componentVersion
        project: $project
    }) {
        componentVersion {
            id
        }
        project {
            id
        }
    }
}
mutation CreateComponentVersion ($component: ID!, $version: String!) {
    createComponentVersion (input: {
        component: $component
        templatedFields: []
        tags: []
        version: $version
    }) {
        componentVersion {
            id
            version
        }
    }
}